interface Lieu {
    id: number,
    nom_lieu?: string
}

interface TypeEnergie {
    id: number,
    designation?: string
}

interface Categorie {
    id: number,
    designation?: string
}

interface BoiteVitesse {
    id: number,
    designation?: string
}